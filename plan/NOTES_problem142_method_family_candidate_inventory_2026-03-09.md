# Notes: Erdős #142 Method-Family Candidate Inventory

Date: 2026-03-09

## Objective

Inventory only those candidate method families that are admissible under the fork-gate standard.

## Excluded Immediately

The following are excluded before any further discussion:

- any variant of the current split-data -> dominance -> matched-profile coupling route;
- any refinement of the current `k = 3` almost-periodicity / Bohr-set density increment body;
- any alternate-transfer claim that does not change the decisive loss profile;
- any packaging or checker-only branch.

## Admissible Candidate Families

### A. Direct asymptotic / counting route

Core idea:

```text
bypass the current split-to-matched-profile coupling layer entirely and target a direct asymptotic
statement for fixed k through counting, supersaturation, or stability-style input.
```

Why admissible:

- it attacks the current coupling bottleneck rather than refining it;
- it does not require the current profile-dominance theorem family;
- it could in principle generate a theorem target closer to `erdos_142 k` itself.

Current weakness:

- no exact theorem-shaped extraction target has yet been isolated in the repository.

### B. Higher-order uniformity / structured-decomposition route

Core idea:

```text
replace the present almost-periodicity -> Bohr-set bootstrap with a different structural engine,
for example one centered on higher-order decomposition or a different inverse-theorem interface.
```

Why admissible:

- it changes the decisive local engine rather than tuning its constants;
- it is not the current `k = 3` recurrence architecture in disguise.

Current weakness:

- still too broad to define one exact theorem candidate without further narrowing.

### C. Ambient-model / transfer replacement route

Core idea:

```text
replace the present integer-to-structured-set transport with a materially different ambient model
or structured object, so that the current Bohr/rank loss analysis no longer controls the route.
```

Why admissible:

- it targets the transfer layer that the critic already isolated as decisive;
- if real, it would invalidate a central closure argument of the current negative architecture.

Current weakness:

- the existing scoped re-audit found no source-backed candidate inside the present modern line;
- so this remains only a speculative fork idea.

## Initial Ranking

For scouting purposes only:

1. direct asymptotic / counting bypass
2. higher-order uniformity / structured-decomposition replacement
3. ambient-model / transfer replacement

Reason:

- the first candidate attacks the already identified coupling dead-end directly;
- the second is broader but less sharply targetable;
- the third is the most speculative because the recent re-audit already found no concrete live
  candidate in the current record.

## Immediate Consequence

```text
Milestone 2 of the scouting plan is complete.
The next question is whether candidate A can be sharpened into one exact theorem-shaped extraction
question, or whether the scouting branch should remain uncommitted.
```
