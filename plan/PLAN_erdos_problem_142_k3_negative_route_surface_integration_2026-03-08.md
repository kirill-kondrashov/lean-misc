# Plan: `k = 3` Negative-Route Surface Integration

Date: 2026-03-08

## Objective

Expose the stabilized post-critic negative route in the repository's public surfaces.

That means:

1. export it through the gap layer;
2. mention it in the README status narrative;
3. keep it clearly separated from the practical source-backed split route and the off-path
   matched-profile frontier.

## Status

Progress: `██████` `3 / 3`

## Milestones

1. `[x]` Add a gap-layer alias/theorem for the stabilized negative `k = 3` route.
   - `K3NegativeRouteStable`
   - `k3_negative_route_stable`

2. `[x]` Update the README current-status section to mention the named negative-route endpoint.

3. `[x]` Link this integration cycle from the stabilization plan so the plan chain stays explicit.

## Current Verdict

```text
The repository now exposes three clearly separated surfaces:
- practical source-backed split route
- off-path matched-profile frontier
- stabilized post-critic negative `k = 3` route
```

## Successor Plan

The next packaging cycle is:

- `PLAN_erdos_problem_142_current_status_packaging_2026-03-08.md`
