# Plan: Erdős #142 Frontier Debt Ledger

Date: 2026-03-08

## Objective

Build the first concrete deliverable required by the revised research program:
an explicit ranked ledger of the remaining mathematical debts.

Each ledger entry must contain:

1. exact theorem target;
2. source status;
3. why the target matters downstream;
4. why the current evidence is positive, negative, or ambiguous;
5. whether the next action should be source audit, route kill, or formal packaging.

## Status

Progress: `█████` `5 / 5`

## Ledger Schema

Each entry should be recorded in the form:

```text
Debt:
Exact target:
Current source status:
Downstream leverage:
Current verdict:
Recommended next action:
```

## Candidate Debts

1. `k = 3` matched-profile strengthening beyond the audited `β = 1 / 12` split route.
2. `k = 4` profile-dominance / coupling debt.
3. `k ≥ 5` profile-dominance / coupling debt.
4. alternative integer-transfer / structured-object escape outside the current negative template.

## Milestones

1. `[x]` Freeze the ledger schema and candidate debt list.

2. `[x]` Fill the `k = 3` matched-profile debt entry.
   - include the reason the present `β = 1 / 12` source-backed route is honest but insufficient

3. `[x]` Fill the `k = 4` and `k ≥ 5` matched-profile debt entries.
   - these are the recommended first audit targets under the critic

4. `[x]` Fill the alternative-transport / template-escape debt entry.
   - must state what would count as a real escape rather than a cosmetic reformulation

5. `[x]` Choose one highest-leverage debt as the next source-first audit cycle.
   - chosen next audit: `PLAN_erdos_problem_142_kge5_matched_profile_debt_audit_2026-03-08.md`

## Current Verdict

```text
The debt ledger is now explicit enough to rank the next live audit target.
The highest-leverage next move is the `k >= 5` matched-profile debt, because it can either:
- kill a mis-specified family target,
- or recover a genuine source-backed family theorem,
and in either case it changes more of the debt profile than another `k = 3` or `k = 4` wrapper cycle.
```

## Ledger Note

- `NOTES_problem142_frontier_debt_ledger_2026-03-08.md`

## Successor Plan

- `PLAN_erdos_problem_142_kge5_matched_profile_debt_audit_2026-03-08.md`
