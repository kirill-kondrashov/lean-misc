# Plan: Erdős #142 `k = 3` Bootstrap `1 / 8` Theorem Program

Date: 2026-03-09

## Objective

Pursue the exact local `k = 3` theorem candidate extracted from the bottleneck notes:
replace the current almost-periodicity-to-structure bootstrapping step by one that propagates the
present architecture from exponent `1 / 9` to exponent `1 / 8`.

## Status

Progress: `████` `4 / 4`

## Active Theorem Question

```text
Can the current architecture support a proved or honestly imported local bootstrapping theorem
with rank increment O(L^5) and local size loss exp(-O(d_j L + L^6)),
thereby yielding a real `1 / 8`-type upper theorem?
```

## Starting Point

- candidate note:
  `NOTES_problem142_k3_bootstrap_one_eighth_theorem_candidate_2026-03-09.md`
- recurrence baseline:
  `NOTES_problem142_k3_upper_recurrence_extraction_2026-03-08.md`
- hold note:
  `NOTES_problem142_k3_matched_profile_hold_2026-03-09.md`

## Milestones

1. `[x]` Freeze the exact local theorem target and its quantitative consequence.

2. `[x]` Separate proof modes.
   - source import
   - local proof from the modern architecture
   - explicit statement that conjectural remarks do not count
   - note:
     `NOTES_problem142_k3_bootstrap_one_eighth_program_scope_2026-03-09.md`

3. `[x]` Define the exact repository endpoint if the theorem is obtained.
   - which `k = 3` split theorem changes
   - which old conjectural `1 / 8` surfaces become live
   - which stronger routes still do not follow

4. `[x]` Decide whether this is a realistic proof program or should be put back on hold.
   - outcome:
     live only as a local-proof-scoping program
   - successor:
     `PLAN_erdos_problem_142_k3_bootstrap_one_eighth_local_proof_scoping_2026-03-09.md`

## Current Verdict

```text
The theorem target is now fully scoped.
It remains live, but only as a local-proof program for a `1 / 8` split improvement.
The next active cycle is to isolate the exact strengthened lemma interface needed for that proof.
```
