# Notes: Erdős #142 `k = 3` Varnavides Conditional Density Transport

Date: 2026-03-09

## Objective

Choose the first narrower direct-counting consequence after the extremal-transport target.

## Selected Consequence

The next exact consequence worth targeting is:

```text
conditional density transport from subscale upper control.
```

In words:

```text
if one has a subscale upper bound

  r3(M) ≤ δ(M) M,

then the Varnavides extremal-transport target should imply

  r3(N) ≤ (δ(M) + 1/M) N

for M ≤ N,

up to the obvious cast/constant formatting.
```

## Why This Is The Right Next Debt

It is the correct next target because it:

- stays strictly below the full direct `1 / 8` theorem;
- uses only the existing extremal-function API;
- turns the imported Varnavides theorem into a reusable reduction lemma;
- isolates the next real mathematical question as:

```text
what subscale upper input, and what scale choice M = M(N),
make this transport nontrivial?
```

## Why The Larger Targets Are Rejected For Now

Not yet:

```text
direct public `1 / 8` theorem surface
```

because that would skip the newly extracted reduction layer.

Not yet:

```text
raw progression-count / T3 API expansion
```

because the current route does not need it to state the first extremal consequence.

## Route Consequence

The active successor should therefore study:

```text
how to package and parameterize the conditional density transport target,
and what first benchmark subscale input should drive it.
```

## Verdict

```text
The extremal-transport encoding cycle is complete.
Its successor is the conditional density transport cycle.
```
