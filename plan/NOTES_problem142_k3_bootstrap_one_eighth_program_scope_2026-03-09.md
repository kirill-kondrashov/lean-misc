# Notes: Erdős #142 `k = 3` Bootstrap `1 / 8` Program Scope

Date: 2026-03-09

## Objective

Freeze the proof-mode split and the exact repository endpoint for the live local `1 / 8`
theorem candidate.

## Proof Modes

### 1. Source import

Status:

```text
closed on the current audited source set
```

Reason:

- `NOTES_problem142_k3_one_eighth_source_audit_2026-03-08.md` found no audited published
  theorem proving the explicit `1 / 8` upper bound.

So this branch may reopen only on new audited source evidence.

### 2. Local proof from the present architecture

Status:

```text
the only live branch
```

Reason:

- the bottleneck notes now isolate one exact theorem-shaped strengthening with a quantified
  `1 / 8` downstream effect.

### 3. Conjectural remarks or omitted optimizations

Status:

```text
not admissible as theorem evidence
```

Reason:

- the hold note already forbids source-motivated remarks or bottleneck diagnoses without a proved
  theorem statement from counting as route-reopen evidence.

## Exact Repository Endpoint

If the local theorem is proved, the immediate repository consequences are:

1. `bound_targets.k3_superpolylog_upper_profile_one_eighth` becomes a live proved target rather
   than conjectural scaffolding.
2. The existing theorem surface
   `erdos_142_three_source_backed_split_one_eighth`
   becomes a live `k = 3` split endpoint rather than an import-only wrapper.
3. The active honest `k = 3` split route can be upgraded from the current `β = 1 / 12` endpoint
   to an explicit `β = 1 / 8` endpoint.

The following still do **not** follow:

1. `import_targets.k3_upper_exponent_gt_half_target`
2. `import_targets.split_gap_k3_profile_dominance_target`
3. any reopen of the old matched-profile closure route

## Scope Verdict

```text
The live theorem program is strictly local.
It targets a `1 / 8` split improvement inside the current architecture.
It is not a source-import program, and it is not a matched-profile program.
```
