# Notes: Erdős #142 `k = 3` Bootstrap `1 / 8` Lemma Encoding Decision

Date: 2026-03-09

## Objective

Decide whether the improved bootstrap lemma target is precise enough to enter the repository as a
theorem surface or only as a placeholder surface.

## Decision

The lemma target is precise enough for a repository-facing placeholder surface, but not for a
theorem surface.

## Reason

The current notes now determine:

- the exact one-step input/output shape;
- the quantitative rank increment target;
- the quantitative local size-loss target;
- the separation between this lemma and the later recurrence-closing theorem.

That is enough structure to justify a named local target in repository notation.

It is **not** enough to justify a theorem surface, because:

- no proof is currently known in the repository;
- no audited published source has been found that states this exact strengthened lemma;
- the March 8 audit already closed the source-import reading of the `1 / 8` route.

## Encoding Policy

So the next honest repository move is:

```text
introduce a placeholder target surface for the improved bootstrap lemma,
clearly marked as local-theorem debt rather than theorem realization.
```

The following should not be done at this stage:

```text
introduce a theorem named as if the lemma were proved;
or promote the full `1 / 8` split route to a checked theorem endpoint.
```

## Consequence

The active successor cycle should design:

1. the exact repository-facing placeholder name for the lemma;
2. the corresponding recurrence-closing target name;
3. the boundary between these placeholder targets and the existing conjectural `1 / 8`
   theorem wrappers.

## Verdict

```text
Outcome: placeholder surface, not theorem surface.
The route remains active, but only through explicitly marked local theorem debt.
```
