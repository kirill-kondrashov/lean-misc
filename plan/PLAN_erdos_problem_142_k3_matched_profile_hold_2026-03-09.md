# Plan: Erdős #142 `k = 3` Matched-Profile Hold

Date: 2026-03-09

## Objective

Follow the debt-ledger recommendation for the `k = 3` strengthening route:
decide whether the stronger matched-profile program should remain an active live debt, or be put
on explicit hold pending genuinely new source evidence.

## Status

Progress: `████` `4 / 4`

## Hold Question

```text
After the audited negative `β > 1 / 2` and `1 / 8` source work,
should the repository still treat
  `import_targets.k3_upper_exponent_gt_half_target`
and
  `import_targets.split_gap_k3_profile_dominance_target`
as active research targets?
```

## Starting Point

- debt-ledger recommendation:
  `route kill / hold unless genuinely new source evidence appears`
- prior audits:
  - `NOTES_problem142_k3_kelley_meka_source_audit_2026-03-07.md`
  - `NOTES_problem142_k3_one_eighth_source_audit_2026-03-08.md`
  - `NOTES_problem142_k3_upper_bottleneck_audit_2026-03-08.md`
- honest current endpoint:
  the `β = 1 / 12` split route

## Milestones

1. `[x]` Freeze the exact strong `k = 3` debt and the prior negative audit state.
   - strong targets:
     - `import_targets.k3_upper_exponent_gt_half_target`
     - `import_targets.split_gap_k3_profile_dominance_target`
   - related conjectural scaffolding:
     - `bound_targets.k3_superpolylog_upper_profile_one_eighth`

2. `[x]` Aggregate the existing audited negative source status.
   - include:
     - current sourced endpoint `β = 1 / 12`
     - negative `1 / 8` audit
     - no audited route to `β > 1 / 2`
   - note: `NOTES_problem142_k3_matched_profile_hold_2026-03-09.md`

3. `[x]` State the precise reopen condition.
   - acceptable reopen evidence:
     a genuinely audited source-backed theorem strong enough to revive the comparison layer
   - note: `NOTES_problem142_k3_matched_profile_hold_2026-03-09.md`

4. `[x]` Produce a hold verdict and repoint the revised program.
   - allowed outcomes:
     - explicit hold pending new sources,
     - or unexpected reopening if the existing evidence was misread
   - outcome: explicit hold pending new sources
   - successor cycle:
     `PLAN_erdos_problem_142_alternate_transfer_scoped_reaudit_2026-03-09.md`

## Current Verdict

```text
The hold decision is now made.

The source-backed `k = 3` endpoint remains the `β = 1 / 12` split route.
Neither the audited `1 / 8` material nor the bottleneck note revives the old matched-profile
comparison target, because there is still no audited source-backed theorem strong enough to close
`import_targets.split_gap_k3_profile_dominance_target`.

So the stronger `k = 3` matched-profile route is no longer an active positive program.
It stays on explicit hold until genuinely new source evidence appears.
```

## Hold Note

- `NOTES_problem142_k3_matched_profile_hold_2026-03-09.md`

## Successor Plan

- `PLAN_erdos_problem_142_alternate_transfer_scoped_reaudit_2026-03-09.md`
